Metadata files are the control plane for the refactored repository.

Files:
- `sample_manifest.csv`: one row per raw sample
- `experiment_map.csv`: one row per experiment or acquisition batch
- `labels.csv`: human or curated labels kept separate from notebooks and scripts
